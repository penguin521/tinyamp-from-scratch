# Results directory

These files contain compact numerical summaries of the completed TinyAMP experiments. They are intentionally small enough to keep under version control.

The raw APD sequences, full generated-sequence tables, and trained checkpoints are not included in this consolidated package.

Run `python scripts/make_figures.py` from the repository root to regenerate the included figures from the CSV summaries.
